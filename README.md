# Escrow App Skeleton

A starter project for an escrow application with:

- **Frontend**: Expo + React Native (Auth flow, Navigation, Context API)
- **Backend**: Node.js + Express (JWT auth + placeholder routes)

## Run

### Backend
```bash
cd server
npm install
npm run dev
